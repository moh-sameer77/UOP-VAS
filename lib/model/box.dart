import 'package:flutter/material.dart';
import 'package:virtual_assistant/model/docment.dart';

class Box {
  String title;
  Color background;
  Document doc;
  Box(this.title, this.background, this.doc);
}
